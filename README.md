# Solana Studios

Studio Visit interest form UI powered by Next.js and Tailwind CSS.

## Getting Started

```bash
pnpm install
pnpm dev
```

Then open `http://localhost:3000` to view the site.

## Deploy

Use `pnpm build` to create a production build. Deploy the generated output with Vercel or your preferred platform.
